@using Kentico.Content.Web.Mvc.PageBuilder
@using Convenience.org
@{
	Layout = "~/Features/Shared/Convenience/_Layout.cshtml";
}

@model TemplateViewModel

@{
	var isPreviewModel = Context.Kentico().PageBuilder().EditMode;
}

@section GoogleAdsBeforeHeader {
	<div>
		@await Html.Kentico().EditableAreaAsync("WidgetAreaBeforeHeader")
	</div>
}

<div class="row">
	@await Component.InvokeAsync("Breadcrumb")
</div>

<div class="row">
	@await Html.Kentico().EditableAreaAsync("HeroWidgetArea")
</div>

<div class="row">
	@await Html.Kentico().EditableAreaAsync("MiddleWidgetArea1")
</div>

<div class="row">
	@await Html.Kentico().EditableAreaAsync("MiddleWidgetArea2")
</div>

<div class="row">
	@await Html.Kentico().EditableAreaAsync("MiddleWidgetArea3")
</div>

<div class="row">
	@await Html.Kentico().EditableAreaAsync("MiddleWidgetArea4")
</div>

<div class="row">
	@await Html.Kentico().EditableAreaAsync("MiddleWidgetArea5")
</div>

<div class="row">
	@await Html.Kentico().EditableAreaAsync("MiddleWidgetArea6")
</div>

<div class="row">
	@await Html.Kentico().EditableAreaAsync("EndWidgetArea")
</div>